configurationDefinitions:
  - platform: KUBERNETESCLUSTER
    type: AgentConfig
    version: 1.0.2
    schema: ./schemas/config.json
    format: yml
    bindings:
      - type: CONFIGURES
        targetType: AGENT
        target: NRJavaAgent
        versions:
          - ">8.0.2"

