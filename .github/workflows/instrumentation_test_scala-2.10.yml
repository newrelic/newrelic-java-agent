name: Branch Build - Scala 2.10 Instrumentation Tests

on:
  push:
    branches-ignore:
      - '**WIP**'
      - 'main'

jobs:
  java-instrumentation-tests:
    uses: newrelic/newrelic-java-agent/.github/workflows/run-gradle-build.yml@main
    with:
      command: ./gradlew --console=plain :instrumentation:test -Ptest8 -PnoInstrumentation -PnoScala -Pscala-2.10 --continue