# AWS Lambda Java Core Events

This instrumentation module parses event metadata sent to an AWS lambda.
The event type determines what transaction agent attributes are added and if it is marked as a web transaction.
