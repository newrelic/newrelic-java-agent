
dependencies {
    implementation(project(":agent-bridge"))
    implementation("org.apache.kafka:kafka-clients:4.0.0")

    testImplementation("org.testcontainers:kafka:1.21.3")
}

jar {
    manifest { attributes 'Implementation-Title': 'com.newrelic.instrumentation.kafka-clients-metrics-4.0.0',
            'Implementation-Title-Alias': 'kafka-clients-metrics' }
}

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(17))
    }
}

verifyInstrumentation {
    passesOnly 'org.apache.kafka:kafka-clients:[4.0.0,)'
}

site {
    title 'Kafka Clients Metrics'
    type 'Kafka'
}
