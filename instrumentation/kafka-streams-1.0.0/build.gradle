jar {
    manifest {
        attributes 'Implementation-Title': 'com.newrelic.instrumentation.kafka-streams-1.0.0',
                'Implementation-Title-Alias': 'kafka-streams_instrumentation'
    }
}

dependencies {
    implementation(project(":agent-bridge"))
    implementation("org.apache.kafka:kafka-streams:1.0.0")
}

verifyInstrumentation {
    passesOnly 'org.apache.kafka:kafka-streams:[1.0.0,2.6.0)'
}

site {
    title 'Kafka-streams'
    type 'Messaging'
}