common: &default_settings
  application_logging:
    enabled: true
    forwarding:
      enabled: true
      max_samples_stored: 2000
    local_decorating:
      enabled: true
    metrics:
      enabled: true

