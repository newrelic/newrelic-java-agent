common: &default_settings
  application_logging:
    enabled: true
    forwarding:
      enabled: true
      max_samples_stored: 10000
      context_data:
        enabled: true
    metrics:
      enabled: true
    local_decorating:
      enabled: false
