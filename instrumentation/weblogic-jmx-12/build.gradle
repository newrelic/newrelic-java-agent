dependencies {
    implementation(project(":agent-bridge"))
    implementation(fileTree(include: ["*.jar"], dir: "lib"))
}

def shouldBuild = fileTree(include: ["*.jar"], dir: "lib").size() > 0

compileJava {
    enabled(shouldBuild)
}

compileTestJava {
    enabled(shouldBuild)
}

jar {
    enabled(shouldBuild)
    manifest { attributes 'Implementation-Title': 'com.newrelic.instrumentation.weblogic-jmx-12' }
}